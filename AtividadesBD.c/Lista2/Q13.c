/*
13) Comente o código abaixo descrevendo suas operações:
 int main(){
      ...
      int i;
      int x; // total de linhas
      int y: // tamanho de cada linha (coluna)
      int **matriz = (int**)(malloc(x*sizeof(int*)));
      for(i=0;i<x;i++)
          matriz[i] = (int*)(malloc(y*sizeof(int)));        
      ...
  }
*/

/*
o código acima aloca uma matriz de inteiros dinamicamente.O x representa o total de linhas e a variável y representa a quantidade de colunas.
*/